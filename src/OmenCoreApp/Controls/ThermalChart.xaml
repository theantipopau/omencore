<UserControl x:Class="OmenCore.Controls.ThermalChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="500">
    <Grid>
        <Border Background="{StaticResource SurfaceDarkBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="1" 
                CornerRadius="6" 
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Rectangle Width="14" Height="14" Fill="{StaticResource AccentBrush}" RadiusX="3" RadiusY="3" />
                        <TextBlock Text="CPU" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="Medium" Margin="6,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Rectangle Width="14" Height="14" Fill="{StaticResource AccentBlueBrush}" RadiusX="3" RadiusY="3" />
                        <TextBlock Text="GPU" Foreground="{StaticResource TextPrimaryBrush}" FontSize="13" FontWeight="Medium" Margin="6,0,0,0" VerticalAlignment="Center" />
                    </StackPanel>
                </StackPanel>
                <Canvas x:Name="ChartCanvas" Grid.Row="1" Background="{StaticResource BackgroundDarkBrush}" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
