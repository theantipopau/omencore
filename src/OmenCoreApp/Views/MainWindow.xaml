<Window x:Class="OmenCore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OmenCore.ViewModels"
        xmlns:views="clr-namespace:OmenCore.Views"
        xmlns:controls="clr-namespace:OmenCore.Controls"
        xmlns:models="clr-namespace:OmenCore.Models"
        mc:Ignorable="d"
        Title="OmenCore - Gaming Laptop Control Center" Height="900" Width="1500" MinHeight="700" MinWidth="1100"
        Icon="/Assets/OmenCore.ico"
        Background="{StaticResource BackgroundDarkBrush}"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display">
    <!-- DataContext will be set via dependency injection in code-behind -->
    
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12">
        <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="40">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0">
                        <Image Source="pack://application:,,,/Assets/logo-small.png" Width="28" Height="28" Margin="0,0,10,0" />
                        <TextBlock Text="OmenCore" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding AppVersionLabel}" FontSize="10" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="8,1,0,0" Opacity="0.8" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,4,0">
                        <Button Content="—" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}" ToolTip="Minimize" FontSize="12" />
                        <Button x:Name="MaximizeButton" Content="☐" Click="MaximizeButton_Click" Style="{StaticResource WindowControlButton}" ToolTip="Maximize" FontSize="12" />
                        <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource WindowCloseButton}" ToolTip="Close" FontSize="12" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="240" MinWidth="220" MaxWidth="300" />
                    <ColumnDefinition Width="*" MinWidth="800" />
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="0,0,0,12">
                    <DockPanel LastChildFill="True" Margin="16,12,16,12">
                        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Center">
                            <Image Source="pack://application:,,,/Assets/logo.png" Width="56" Height="56" Stretch="Uniform" />
                            <TextBlock Text="OmenCore" Foreground="{StaticResource TextPrimaryBrush}" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,6,0,0" />
                            <TextBlock Text="Gaming Control" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" HorizontalAlignment="Center" Margin="0,2,0,0" />
                            
                            <!-- System Status Indicator -->
                            <Border Background="{StaticResource SurfaceMediumBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="6"
                                    Padding="8,6"
                                    Margin="0,6,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,6">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" Data="{StaticResource IconPerformance}" Width="14" Height="14" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="PERFORMANCE" FontSize="8" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                            <TextBlock Text="{Binding Dashboard.CurrentPerformanceMode, FallbackValue=Auto}" 
                                                       FontSize="11" FontWeight="Bold" 
                                                       Foreground="{StaticResource AccentBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,4"/>
                                    
                                    <Grid Margin="0,6,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" Data="{StaticResource IconFan}" Width="14" Height="14" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,6,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="FAN MODE" FontSize="8" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                            <TextBlock Text="{Binding Dashboard.CurrentFanMode, FallbackValue=Auto}" 
                                                       FontSize="11" FontWeight="Bold" 
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                            <Button Command="{Binding CheckForUpdatesCommand}" Style="{StaticResource SecondaryButton}" Height="32" Margin="0,0,0,5" ToolTip="Manually check for a new version">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconUpdate}" Width="14" Height="14" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="Check for Updates" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding OpenGameProfileManagerCommand}" Style="{StaticResource SecondaryButton}" Height="32" Margin="0,0,0,5" ToolTip="Manage per-game settings">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconGamepad}" Width="14" Height="14" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="Game Profiles" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ExportConfigurationCommand}" Style="{StaticResource SecondaryButton}" Height="32" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconExport}" Width="14" Height="14" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="Export Config" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ImportConfigurationCommand}" Style="{StaticResource SecondaryButton}" Height="32" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconImport}" Width="14" Height="14" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="Import Config" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding OpenAboutCommand}" Style="{StaticResource SecondaryButton}" Height="32">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconAbout}" Width="14" Height="14" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                    <TextBlock Text="About" FontSize="11" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <ScrollViewer VerticalScrollBarVisibility="Auto" 
                                      ScrollViewer.CanContentScroll="True"
                                      ScrollViewer.IsDeferredScrollingEnabled="False"
                                      VirtualizingPanel.ScrollUnit="Pixel">
                            <StackPanel>
                                <TextBlock Text="QUICK ACTIONS" Foreground="{StaticResource TextTertiaryBrush}" FontSize="9" FontWeight="SemiBold" Margin="0,0,0,6" />
                                <Button Command="{Binding ApplyFanPresetCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,5" HorizontalContentAlignment="Left" Padding="8,6" Height="32">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconFan}" Width="14" Height="14" Fill="{StaticResource TextPrimaryBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                        <TextBlock Text="Apply Fan Preset" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding ApplyPerformanceModeCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,5" HorizontalContentAlignment="Left" Padding="8,6" Height="32">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconPerformance}" Width="14" Height="14" Fill="{StaticResource WarningBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                        <TextBlock Text="Performance Mode" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding ApplyLightingProfileCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,5" HorizontalContentAlignment="Left" Padding="8,6" Height="32">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconLighting}" Width="14" Height="14" Fill="{StaticResource AccentPurpleBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                        <TextBlock Text="Apply Lighting" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding GamingModeCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,5" HorizontalContentAlignment="Left" Padding="8,6" Height="32">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconGamepad}" Width="14" Height="14" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                        <TextBlock Text="Gaming Mode" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding RestoreDefaultsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,10,0,0" HorizontalContentAlignment="Left" Padding="8,6" Height="32">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconRestore}" Width="14" Height="14" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,8,0"/>
                                        <TextBlock Text="Restore Defaults" FontSize="11" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                
                                <!-- System Information -->
                                <TextBlock Text="SYSTEM INFO" Foreground="{StaticResource TextTertiaryBrush}" FontSize="9" FontWeight="SemiBold" Margin="0,12,0,6" />
                                <Border Style="{StaticResource SurfaceCard}" Padding="10">
                                    <StackPanel>
                                        <!-- CPU -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Image Grid.Column="0" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Source" Value="pack://application:,,,/Assets/intel.png" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding SystemInfo.CpuVendor}" Value="AMD">
                                                                <Setter Property="Source" Value="pack://application:,,,/Assets/amd.png" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="CPU" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding SystemInfo.CpuName}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- RAM -->
                                        <Grid Margin="0,0,0,8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="24" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="0" Data="{StaticResource IconRam}" Width="18" Height="18" Fill="{StaticResource AccentPurpleBrush}" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="RAM" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding SystemInfo.RamSizeFormatted}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" />
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- GPU(s) -->
                                        <ItemsControl ItemsSource="{Binding SystemInfo.Gpus}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,8">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="24" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Image Grid.Column="0" Width="20" Height="20" VerticalAlignment="Center" Margin="0,0,8,0">
                                                            <Image.Style>
                                                                <Style TargetType="Image">
                                                                    <Setter Property="Source" Value="/Assets/nvidia.png" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Vendor}" Value="AMD">
                                                                            <Setter Property="Source" Value="/Assets/amd.png" />
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Vendor}" Value="Intel">
                                                                            <Setter Property="Source" Value="/Assets/intel.png" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Image.Style>
                                                        </Image>
                                                        <StackPanel Grid.Column="1">
                                                            <TextBlock Text="GPU" Foreground="{StaticResource TextTertiaryBrush}" FontSize="10" FontWeight="SemiBold" />
                                                            <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- Main Content -->
                <Grid Grid.Column="1" Background="{StaticResource BackgroundDarkBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="240" />
                    </Grid.RowDefinitions>

                    <!-- Update Banner + Non-HP Warning -->
                    <StackPanel Grid.Row="0">
                        <Border Background="#221FC3FF"
                                BorderBrush="{StaticResource AccentBlueBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                Margin="24,12,24,8"
                                Visibility="{Binding UpdateBannerVisible, Converter={StaticResource BoolToVisibility}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <!-- Update Banner Header -->
                                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">
                                    <Path Data="{StaticResource IconUpdate}" Width="20" Height="20" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <StackPanel>
                                        <TextBlock Text="Update Ready" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{Binding UpdateBannerMessage}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Buttons -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Install Update"
                                            Command="{Binding InstallUpdateCommand}"
                                            Style="{StaticResource ModernButton}"
                                            Width="140"
                                            Height="34"
                                            Margin="0,0,8,0" />
                                    <Button Content="Release Notes"
                                            Command="{Binding OpenReleaseNotesCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Width="130"
                                            Height="34" />
                                </StackPanel>
                                
                                <!-- Download Progress -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                            Margin="34,12,0,0"
                                            Visibility="{Binding UpdateDownloadInProgress, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                                    <ProgressBar Value="{Binding UpdateDownloadProgress, Mode=OneWay}" 
                                                 Minimum="0" Maximum="100" 
                                                 Height="8" 
                                                 BorderThickness="0"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 Background="{StaticResource SurfaceDarkBrush}"
                                                 Margin="0,0,0,6" />
                                    <TextBlock Text="{Binding UpdateDownloadStatus, Mode=OneWay}" 
                                               FontSize="12" 
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               FontFamily="Consolas" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Background="#44FF9800" 
                                BorderBrush="#FF9800" 
                                BorderThickness="2" 
                                CornerRadius="8" 
                                Padding="14,10" 
                                Margin="24,12,24,12"
                                Visibility="{Binding SystemInfo.IsHpOmen, Converter={StaticResource InverseBoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconWarning}" Width="22" Height="22" Fill="#FF9800" Stretch="Uniform" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <StackPanel>
                                    <TextBlock Text="Warning: Non-HP Omen System Detected" 
                                               FontWeight="Bold" 
                                               Foreground="#FFFFFF" 
                                               FontSize="14" />
                                    <TextBlock Text="This application is designed for HP Omen laptops. Some features may not work correctly on your system." 
                                               TextWrapping="Wrap" 
                                               Foreground="#FFFFFF" 
                                               FontSize="12" 
                                               Margin="0,3,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        
                        <!-- Dashboard Header -->
                        <Border Margin="24,0,24,16" Style="{StaticResource SurfaceCard}">
                            <StackPanel>
                                <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                <TextBlock Text="Manage thermals, power, lighting, and peripherals" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0" FontSize="13" Opacity="0.9" />
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <TabControl Grid.Row="1" Margin="24,0,24,16" Style="{StaticResource ModernTabControl}" ItemContainerStyle="{StaticResource ModernTabItem}">
                <TabItem Header="HP Omen">
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <StackPanel Margin="20,20,20,40">
                            <Border Style="{StaticResource SurfaceCard}" 
                                    Padding="16,12"
                                    Margin="0,0,0,20"
                                    HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="pack://application:,,,/Assets/omen.png" Width="48" Height="48" Margin="0,0,16,0" VerticalAlignment="Center" />
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="HP OMEN Control" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="Advanced Laptop Configuration" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" />
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                            
                            <!-- Fan Control View -->
                            <GroupBox Header="Fan &amp; Thermal Control" Style="{StaticResource ModernGroupBox}">
                                <views:FanControlView DataContext="{Binding FanControl}" />
                            </GroupBox>
                            
                            <!-- System Control View -->
                            <GroupBox Header="System Control" Style="{StaticResource ModernGroupBox}">
                                <views:SystemControlView DataContext="{Binding SystemControl}" />
                            </GroupBox>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
                
                <!-- Monitoring Tab with Dashboard View -->
                <TabItem Header="Monitoring">
                    <views:DashboardView DataContext="{Binding Dashboard}" />
                </TabItem>
                
                <!-- Peripherals Tab with Lighting View -->
                <TabItem Header="RGB &amp; Peripherals">
                    <views:LightingView DataContext="{Binding Lighting}" />
                </TabItem>
                
                <!-- Settings Tab -->
                <TabItem Header="Settings">
                    <views:SettingsView DataContext="{Binding Settings}" />
                </TabItem>
            </TabControl>

            <!-- Footer: Recent Activity and System Log -->
            <Grid Grid.Row="2" Margin="24,0,24,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,12,0">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="RECENT ACTIVITY" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,12" />
                        <ListBox ItemsSource="{Binding RecentEvents}" Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" FontFamily="Consolas" FontSize="13" 
                                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.CacheLength="5" VirtualizingPanel.CacheLengthUnit="Item" />
                    </DockPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorder}">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="SYSTEM LOG" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,12" />
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Background="Transparent">
                            <TextBlock Text="{Binding LogBuffer}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="Consolas" FontSize="11" LineHeight="16" />
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
