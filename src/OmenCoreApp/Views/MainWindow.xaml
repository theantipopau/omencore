<Window x:Class="OmenCore.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:OmenCore.ViewModels"
        xmlns:views="clr-namespace:OmenCore.Views"
        xmlns:controls="clr-namespace:OmenCore.Controls"
        xmlns:models="clr-namespace:OmenCore.Models"
        xmlns:utils="clr-namespace:OmenCore.Utils"
        mc:Ignorable="d"
        Title="OmenCore - Gaming Laptop Control Center" Height="900" Width="1500" MinHeight="500" MinWidth="800"
        Icon="/Assets/OmenCore.ico"
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        AutomationProperties.Name="OmenCore Gaming Laptop Control Center">
    
    <!-- Keyboard Shortcuts for refresh -->
    <Window.InputBindings>
        <KeyBinding Key="F5" Command="{Binding CheckForUpdatesCommand}" />
    </Window.InputBindings>
    
    <!-- DataContext will be set via dependency injection in code-behind -->
    
    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12"
            Background="{StaticResource BackgroundDarkBrush}">
        <Border.Effect>
            <!-- Subtle shadow for depth on systems that support it -->
            <DropShadowEffect BlurRadius="15" ShadowDepth="2" Direction="270" Color="#000000" Opacity="0.3" />
        </Border.Effect>
        <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
            
            <!-- Custom Title Bar -->
            <Border Grid.Row="0" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="12,12,0,0" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid Height="44">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0">
                        <Image Source="pack://application:,,,/Assets/logo-small.png" Width="26" Height="26" Margin="0,0,10,0" RenderOptions.BitmapScalingMode="HighQuality" />
                        <TextBlock Text="OmenCore" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" VerticalAlignment="Center" />
                        <Border Background="{StaticResource SurfaceMediumBrush}" CornerRadius="4" Padding="6,2" Margin="10,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="{Binding AppVersionLabel}" FontSize="10" Foreground="{StaticResource AccentBrush}" FontWeight="Medium" />
                        </Border>
                        
                        <!-- Update Available Indicator -->
                        <Border Background="#1A4CAF50" BorderBrush="#4CAF50" BorderThickness="1" CornerRadius="4" Padding="8,2" Margin="10,0,0,0" VerticalAlignment="Center"
                                Visibility="{Binding UpdateAvailable, Converter={StaticResource BoolToVisibility}}"
                                Cursor="Hand"
                                ToolTip="Click to download the latest version">
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick" Command="{Binding OpenUpdateUrlCommand}" />
                            </Border.InputBindings>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="🔔" FontSize="11" Margin="0,0,4,0" VerticalAlignment="Center"/>
                                <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="#4CAF50" VerticalAlignment="Center">
                                    <Run Text="Update:"/>
                                    <Run Text="{Binding UpdateVersion}"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,4,0">
                        <Button Content="—" Click="MinimizeButton_Click" Style="{StaticResource WindowControlButton}" ToolTip="Minimize" FontSize="12" />
                        <Button x:Name="MaximizeButton" Content="☐" Click="MaximizeButton_Click" Style="{StaticResource WindowControlButton}" ToolTip="Maximize" FontSize="12" />
                        <Button Content="✕" Click="CloseButton_Click" Style="{StaticResource WindowCloseButton}" ToolTip="Close" FontSize="12" />
                    </StackPanel>
                </Grid>
            </Border>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="260" MinWidth="200" MaxWidth="300" />
                    <ColumnDefinition Width="*" MinWidth="600" />
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="{StaticResource SurfaceDarkBrush}" CornerRadius="0,0,0,12">
                    <DockPanel LastChildFill="True" Margin="12,10,12,10">
                        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Center">
                            <Image Source="pack://application:,,,/Assets/logo.png" Width="56" Height="56" Stretch="Uniform" />
                            <TextBlock Text="OmenCore" Foreground="{StaticResource TextPrimaryBrush}" FontSize="15" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,6,0,0" />
                            <TextBlock Text="Gaming Control" Foreground="{StaticResource TextTertiaryBrush}" FontSize="9" HorizontalAlignment="Center" Margin="0,2,0,0" />
                            
                            <!-- System Status Indicator -->
                            <Border Background="{StaticResource SurfaceMediumBrush}"
                                    BorderBrush="{StaticResource AccentBrush}"
                                    BorderThickness="1"
                                    CornerRadius="5"
                                    Padding="6,5"
                                    Margin="0,8,0,0">
                                <StackPanel>
                                    <Grid Margin="0,0,0,4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" Data="{StaticResource IconPerformance}" Width="12" Height="12" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="PERFORMANCE" FontSize="7" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                            <TextBlock Text="{Binding Dashboard.CurrentPerformanceMode, FallbackValue=Auto}" 
                                                       FontSize="10" FontWeight="Bold" 
                                                       Foreground="{StaticResource AccentBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                    
                                    <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,2"/>
                                    
                                    <Grid Margin="0,4,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Path Grid.Column="0" Data="{StaticResource IconFan}" Width="12" Height="12" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,5,0" VerticalAlignment="Center"/>
                                        <StackPanel Grid.Column="1">
                                            <TextBlock Text="FAN MODE" FontSize="7" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}"/>
                                            <TextBlock Text="{Binding Dashboard.CurrentFanMode, FallbackValue=Auto}" 
                                                       FontSize="10" FontWeight="Bold" 
                                                       Foreground="{StaticResource TextPrimaryBrush}"/>
                                        </StackPanel>
                                    </Grid>
                                </StackPanel>
                            </Border>
                            
                            <!-- Capability Warning Banner (Desktop/Fan unavailable) -->
                            <Border Background="#2D2D1F"
                                    BorderBrush="#FF9800"
                                    BorderThickness="1"
                                    CornerRadius="5"
                                    Padding="6,5"
                                    Margin="0,5,0,0"
                                    Visibility="{Binding CapabilityWarning, Converter={StaticResource NullToVisibilityConverter}, FallbackValue=Collapsed}">
                                <StackPanel>
                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,3">
                                        <TextBlock Text="⚠" FontSize="10" Foreground="#FF9800" Margin="0,0,5,0"/>
                                        <TextBlock Text="LIMITED MODE" FontSize="7" FontWeight="Bold" Foreground="#FF9800"/>
                                    </StackPanel>
                                    <TextBlock Text="{Binding CapabilityWarning}" 
                                               FontSize="8" 
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               TextWrapping="Wrap"
                                               MaxWidth="160"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                        <StackPanel DockPanel.Dock="Bottom" Margin="0,12,0,0">
                            <Button Command="{Binding CheckForUpdatesCommand}" Style="{StaticResource SecondaryButton}" Height="30" Margin="0,0,0,5" ToolTip="Manually check for a new version">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconUpdate}" Width="12" Height="12" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Check for Updates" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding OpenGameProfileManagerCommand}" Style="{StaticResource SecondaryButton}" Height="30" Margin="0,0,0,5" ToolTip="Manage per-game settings">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconGamepad}" Width="12" Height="12" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Game Profiles" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ExportConfigurationCommand}" Style="{StaticResource SecondaryButton}" Height="30" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconExport}" Width="12" Height="12" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Export Config" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding ImportConfigurationCommand}" Style="{StaticResource SecondaryButton}" Height="30" Margin="0,0,0,5">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconImport}" Width="12" Height="12" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="Import Config" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                            <Button Command="{Binding OpenAboutCommand}" Style="{StaticResource SecondaryButton}" Height="30">
                                <StackPanel Orientation="Horizontal">
                                    <Path Data="{StaticResource IconAbout}" Width="12" Height="12" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                    <TextBlock Text="About" FontSize="10" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                        <ScrollViewer Style="{StaticResource SmoothScrollViewer}" utils:ScrollSpeedBehavior.ScrollSpeedMultiplier="1.2">
                            <StackPanel>
                                <TextBlock Text="QUICK ACTIONS" Foreground="{StaticResource TextTertiaryBrush}" FontSize="8" FontWeight="SemiBold" Margin="0,4,0,6" />
                                <Button Command="{Binding ApplyFanPresetCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,3" HorizontalContentAlignment="Left" Padding="6,4" Height="26">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconFan}" Width="12" Height="12" Fill="{StaticResource TextPrimaryBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                        <TextBlock Text="Apply Fan Preset" FontSize="10" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding ApplyPerformanceModeCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,3" HorizontalContentAlignment="Left" Padding="6,4" Height="26">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconPerformance}" Width="12" Height="12" Fill="{StaticResource WarningBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                        <TextBlock Text="Performance Mode" FontSize="10" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding ApplyLightingProfileCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,3" HorizontalContentAlignment="Left" Padding="6,4" Height="26">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconLighting}" Width="12" Height="12" Fill="{StaticResource AccentPurpleBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                        <TextBlock Text="Apply Lighting" FontSize="10" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding GamingModeCommand}" Style="{StaticResource ModernButton}" Margin="0,0,0,3" HorizontalContentAlignment="Left" Padding="6,4" Height="26">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconGamepad}" Width="12" Height="12" Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                        <TextBlock Text="Gaming Mode" FontSize="10" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <Button Command="{Binding RestoreDefaultsCommand}" Style="{StaticResource SecondaryButton}" Margin="0,6,0,0" HorizontalContentAlignment="Left" Padding="6,4" Height="26">
                                    <StackPanel Orientation="Horizontal">
                                        <Path Data="{StaticResource IconRestore}" Width="12" Height="12" Fill="{StaticResource TextSecondaryBrush}" Stretch="Uniform" Margin="0,0,6,0"/>
                                        <TextBlock Text="Restore Defaults" FontSize="10" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                
                                <!-- System Information -->
                                <TextBlock Text="SYSTEM INFO" Foreground="{StaticResource TextTertiaryBrush}" FontSize="8" FontWeight="SemiBold" Margin="0,14,0,6" />
                                <Border Style="{StaticResource SurfaceCard}" Padding="8">
                                    <StackPanel>
                                        <!-- HP OMEN Model (shown for HP gaming systems) -->
                                        <Grid Margin="0,0,0,6" Visibility="{Binding SystemInfo.IsHpGaming, Converter={StaticResource BoolToVisibility}, FallbackValue=Collapsed}">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Image Grid.Column="0" Source="pack://application:,,,/Assets/omen.png" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="HP OMEN" Foreground="{StaticResource AccentBrush}" FontSize="8" FontWeight="Bold" />
                                                <TextBlock Text="{Binding SystemInfo.ModelName, FallbackValue=HP OMEN Laptop}" 
                                                           Foreground="{StaticResource TextPrimaryBrush}" 
                                                           FontSize="9" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- CPU -->
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Image Grid.Column="0" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0">
                                                <Image.Style>
                                                    <Style TargetType="Image">
                                                        <Setter Property="Source" Value="pack://application:,,,/Assets/intel.png" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding SystemInfo.CpuVendor}" Value="AMD">
                                                                <Setter Property="Source" Value="pack://application:,,,/Assets/amd.png" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Image.Style>
                                            </Image>
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="CPU" Foreground="{StaticResource TextTertiaryBrush}" FontSize="8" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding SystemInfo.CpuName}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="9" TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- RAM -->
                                        <Grid Margin="0,0,0,6">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="20" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="0" Data="{StaticResource IconRam}" Width="14" Height="14" Fill="{StaticResource AccentPurpleBrush}" Stretch="Uniform" VerticalAlignment="Center" Margin="0,0,6,0" />
                                            <StackPanel Grid.Column="1">
                                                <TextBlock Text="RAM" Foreground="{StaticResource TextTertiaryBrush}" FontSize="8" FontWeight="SemiBold" />
                                                <TextBlock Text="{Binding SystemInfo.RamSizeFormatted}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="9" />
                                            </StackPanel>
                                        </Grid>
                                        
                                        <!-- GPU(s) -->
                                        <ItemsControl ItemsSource="{Binding SystemInfo.Gpus}">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Grid Margin="0,0,0,6">
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="20" />
                                                            <ColumnDefinition Width="*" />
                                                        </Grid.ColumnDefinitions>
                                                        <Image Grid.Column="0" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,6,0">
                                                            <Image.Style>
                                                                <Style TargetType="Image">
                                                                    <Setter Property="Source" Value="/Assets/nvidia.png" />
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding Vendor}" Value="AMD">
                                                                            <Setter Property="Source" Value="/Assets/amd.png" />
                                                                        </DataTrigger>
                                                                        <DataTrigger Binding="{Binding Vendor}" Value="Intel">
                                                                            <Setter Property="Source" Value="/Assets/intel.png" />
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </Image.Style>
                                                        </Image>
                                                        <StackPanel Grid.Column="1">
                                                            <TextBlock Text="GPU" Foreground="{StaticResource TextTertiaryBrush}" FontSize="8" FontWeight="SemiBold" />
                                                            <TextBlock Text="{Binding Name}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="9" TextWrapping="Wrap" />
                                                        </StackPanel>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                    </DockPanel>
                </Border>

                <!-- Main Content -->
                <Grid Grid.Column="1" Background="{StaticResource BackgroundDarkBrush}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="160" />
                    </Grid.RowDefinitions>

                    <!-- Update Banner + Non-HP Warning -->
                    <StackPanel Grid.Row="0">
                        <Border Background="#221FC3FF"
                                BorderBrush="{StaticResource AccentBlueBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                Margin="24,12,24,8"
                                Visibility="{Binding UpdateBannerVisible, Converter={StaticResource BoolToVisibility}}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <!-- Update Banner Header -->
                                <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">
                                    <Path Data="{StaticResource IconUpdate}" Width="20" Height="20" Fill="{StaticResource AccentBlueBrush}" Stretch="Uniform" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                    <StackPanel>
                                        <TextBlock Text="Update Ready" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
                                        <TextBlock Text="{Binding UpdateBannerMessage}" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" TextWrapping="Wrap" />
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Buttons -->
                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                    <Button Content="Install Update"
                                            Command="{Binding InstallUpdateCommand}"
                                            Style="{StaticResource ModernButton}"
                                            Width="140"
                                            Height="34"
                                            Margin="0,0,8,0" />
                                    <Button Content="Release Notes"
                                            Command="{Binding OpenReleaseNotesCommand}"
                                            Style="{StaticResource SecondaryButton}"
                                            Width="130"
                                            Height="34" />
                                </StackPanel>
                                
                                <!-- Download Progress -->
                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" 
                                            Margin="34,12,0,0"
                                            Visibility="{Binding UpdateDownloadInProgress, Mode=OneWay, Converter={StaticResource BoolToVisibility}}">
                                    <ProgressBar Value="{Binding UpdateDownloadProgress, Mode=OneWay}" 
                                                 Minimum="0" Maximum="100" 
                                                 Height="8" 
                                                 BorderThickness="0"
                                                 Foreground="{StaticResource AccentBlueBrush}"
                                                 Background="{StaticResource SurfaceDarkBrush}"
                                                 Margin="0,0,0,6" />
                                    <TextBlock Text="{Binding UpdateDownloadStatus, Mode=OneWay}" 
                                               FontSize="12" 
                                               Foreground="{StaticResource TextSecondaryBrush}"
                                               FontFamily="Consolas" />
                                </StackPanel>
                            </Grid>
                        </Border>
                        <Border Background="#44FF9800" 
                                BorderBrush="#FF9800" 
                                BorderThickness="2" 
                                CornerRadius="8" 
                                Padding="14,10" 
                                Margin="24,12,24,12"
                                Visibility="{Binding SystemInfo.IsHpGaming, Converter={StaticResource InverseBoolToVisibility}}">
                            <StackPanel Orientation="Horizontal">
                                <Path Data="{StaticResource IconWarning}" Width="22" Height="22" Fill="#FF9800" Stretch="Uniform" Margin="0,0,14,0" VerticalAlignment="Center"/>
                                <StackPanel>
                                    <TextBlock Text="Warning: Non-HP Gaming System Detected" 
                                               FontWeight="Bold" 
                                               Foreground="#FFFFFF" 
                                               FontSize="14" />
                                    <TextBlock Text="This application is designed for HP OMEN and Victus gaming laptops. Some features may not work correctly on your system." 
                                               TextWrapping="Wrap" 
                                               Foreground="#FFFFFF" 
                                               FontSize="12" 
                                               Margin="0,3,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <TabControl Grid.Row="1" Margin="24,0,24,12" Style="{StaticResource ModernTabControl}" ItemContainerStyle="{StaticResource ModernTabItem}">
                <!-- General Tab: Performance + Fan Profiles (Paired) -->
                <TabItem Header="General">
                    <views:GeneralView DataContext="{Binding General}" />
                </TabItem>
                
                <!-- Advanced Tab: CPU Undervolt, GPU Settings, Custom Fan Curves -->
                <TabItem Header="Advanced">
                    <views:AdvancedView DataContext="{Binding}" />
                </TabItem>
                
                <!-- Monitoring Tab with Dashboard View -->
                <TabItem Header="Monitoring">
                    <views:DashboardView DataContext="{Binding Dashboard}" />
                </TabItem>
                
                <!-- System Optimizer Tab - Windows Gaming Tweaks -->
                <TabItem Header="Optimizer">
                    <views:SystemOptimizerView DataContext="{Binding SystemOptimizer}" />
                </TabItem>
                
                <!-- Bloatware Manager Tab -->
                <TabItem Header="Bloatware">
                    <views:BloatwareManagerView DataContext="{Binding BloatwareManager}" />
                </TabItem>
                
                <!-- Peripherals Tab with Lighting View -->
                <TabItem Header="RGB &amp; Peripherals" Visibility="{Binding Lighting, Converter={StaticResource NullToVisibilityConverter}}">
                    <views:LightingView DataContext="{Binding Lighting}" />
                </TabItem>
                
                <!-- Settings Tab -->
                <TabItem Header="Settings">
                    <views:SettingsView DataContext="{Binding Settings}" />
                </TabItem>
            </TabControl>

            <!-- Footer: Recent Activity and System Log -->
            <Grid Grid.Row="2" Margin="24,0,24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>
                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,8,0" Padding="12,8">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="RECENT ACTIVITY" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,8" />
                        <ListBox ItemsSource="{Binding RecentEvents}" Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}" BorderThickness="0" FontFamily="Consolas" FontSize="11" 
                                 VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" VirtualizingPanel.CacheLength="5" VirtualizingPanel.CacheLengthUnit="Item" />
                    </DockPanel>
                </Border>
                <Border Grid.Column="1" Style="{StaticResource CardBorder}" Padding="12,8">
                    <DockPanel>
                        <TextBlock DockPanel.Dock="Top" Text="SYSTEM LOG" FontSize="10" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,8" />
                        <ScrollViewer Style="{StaticResource SmoothScrollViewer}">
                            <TextBlock Text="{Binding LogBuffer}" TextWrapping="Wrap" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="Consolas" FontSize="10" LineHeight="14" />
                        </ScrollViewer>
                    </DockPanel>
                </Border>
            </Grid>
        </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
